# Development Caddy configuration

Place the development environment `Caddyfile` in this directory only after its routes and upstreams are known. Development is the first integration environment and may not deploy directly to staging or production without promotion PRs.
