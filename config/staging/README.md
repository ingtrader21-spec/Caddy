# Staging Caddy configuration

This directory contains the pre-production configuration promoted from `test`. Staging is where TLS, routing, upstream health, authentication boundaries, redirects, WebSockets, and smoke tests are verified before production approval.
