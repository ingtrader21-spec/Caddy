# Test Caddy configuration

This directory contains the configuration promoted from `development` for automated and integration testing. Changes arrive here through a pull request; do not bypass the development validation gate.
