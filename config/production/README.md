# Production Caddy configuration

`config/production/Caddyfile` is the only configuration accepted by `scripts/deploy-production.sh`. It must be promoted from `staging` through a reviewed PR. Until the real live configuration has been imported, validated, and reviewed, production deployment remains intentionally blocked.
