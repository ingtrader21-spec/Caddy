# Bounded runtime stop conditions

Stop before the production job, or fail the active job, on any source, digest, configuration, signature, attestation, runtime user, capability, listener, health, certificate, HTTP/2, HTTP/3, WebSocket, Kong, Keycloak, mTLS, denial, request-limit, upstream, log-redaction, or pre/post snapshot mismatch.
