# Bounded runtime audit trail

GitHub Actions artifacts from each phase are the certification record. The staging artifact must contain the immutable tuple and bounded probe results; the production artifact must contain pre- and post-canary fixed-target runtime evidence plus the read-only probe report.
