# Bounded runtime result standard

Only `PASS` from both ordered jobs constitutes runtime certification. A queued runner, missing protected path, failed immutable identity check, skipped job, cancelled run, or incomplete artifact is not a pass and must hold promotion.
