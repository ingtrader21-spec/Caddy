# Bounded runtime certification scope

Certification covers the signed edge image, its embedded canonical configuration, the bounded staging network behavior, and the existing production Caddy container under read-only probes. It does not authorize live configuration replacement or application writes.
